# forge

create go projects

## Usage

```bash
./forge --help
Usage of ./forge:
  -g    initialize a git repository
  -k string
        kind of the project. kinds: cli, api, app (default "cli")
  -n string
        name of the project
```
